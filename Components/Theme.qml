pragma Singleton
import QtQuick

// ─────────────────────────────────────────────────────────────────────────────
// Theme — auto-generated by wallpaper-set.sh
// Wallpaper: /home/sassech/Imágenes/GDA14X7WUAANFL1.png
// Re-run  ~/.config/quickshell/scripts/wallpaper-set.sh  to update.
// ─────────────────────────────────────────────────────────────────────────────
QtObject {

    // ── Wallpaper-derived (Material You dark scheme) ──────────────────────
    readonly property color base:         "#111318"
    readonly property color surface1:     "#1A1B20"
    readonly property color surface2:     "#1E1F25"
    readonly property color surface3:     "#282A2F"
    readonly property color surface4:     "#33353A"
    readonly property color overlay:      "#37393E"
    readonly property color muted3:       "#44474F"
    readonly property color muted2:       "#8E9099"
    readonly property color muted1:       "#C4C6D0"
    readonly property color text:         "#E2E2E9"
    readonly property color accent:       "#ADC6FF"
    readonly property color accentDim:    "#2B4678"
    readonly property color accent2:      "#BFC6DC"

    // ── Semantic (also wallpaper-aware) ───────────────────────────────────
    readonly property color error:        "#FFB4AB"
    readonly property color warning:      "#DEBCDF"
    readonly property color yellow:       "#F9D492"   // no M3 equivalent — fixed
    readonly property color success:      "#8BD4A4"   // no M3 equivalent — fixed
    readonly property color sky:          "#88C9D4"   // no M3 equivalent — fixed

    // ── Computed alpha variants (auto-update from above) ──────────────────
    readonly property color dim:            Qt.rgba(base.r, base.g, base.b, 0.80)
    readonly property color cardBg:         Qt.rgba(base.r, base.g, base.b, 0.91)
    readonly property color cardBg2:        Qt.rgba(base.r, base.g, base.b, 0.60)
    readonly property color accentSurface:  Qt.tint(surface2, Qt.rgba(accent.r,  accent.g,  accent.b,  0.22))
    readonly property color successSurface: Qt.tint(surface2, Qt.rgba(success.r, success.g, success.b, 0.22))
    readonly property color hover:          Qt.rgba(1, 1, 1, 0.08)
    readonly property color hover2:         Qt.rgba(1, 1, 1, 0.20)
}
